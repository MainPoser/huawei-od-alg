文章特征提取

一篇文档由多个文本片段组成（这里假定每个片段由小写英文字母组成，不包含其他内容），需要针对该文档的多个片段进行特征提取，提取方法如下：

特征提取处理时首先找出所有片段中都包含的字母，也就是该字母在所有片段中都至少出现过一次。
如果某个字母在多个片段中都出现过，则计算字母在所有片段中出现的最小次数，如果最小次数为1，则特征中包含1个该字母，如果最小次数为2，则特征中包含2个该字母，以此类推。
最终输出的文本特征是具备上述条件所有字母的集合，且字母按照从小到大排序。
如果所有的字母都不符合该条件，则提取特征的结果为空。
补充说明：用例中字符串 Q（即片段）的总数小于1000个

输入描述
空格分割的文本段

输出描述
输出特征提取结果

示例1

输入

paper parent parade
输出

aepr
说明

“p”, “a”, “e”, “l” 在每个字符串中都至少出现一次。
每个字母在各字符串中都至少出现1次，所以结果中每个字母出现一次。